---
import SidebarLayout from "@/layouts/SidebarLayout.astro";
import { teamMembers } from "@/lib/constants/team";
import LinkedinIcon from "@/icons/linkedin.svg";

const member = teamMembers.find((m) => m.username === "liz")!;

const achievements = [
  { icon: "🎯", title: "Projects Delivered", value: "15+" },
  { icon: "👥", title: "Team Members Led", value: "8" },
  { icon: "📈", title: "On-time Delivery", value: "98%" },
];
---

<SidebarLayout
  title={`${member.name} | Crafter Station`}
  description={`${member.name} - ${member.position} at Crafter Station`}
>
  <div class="max-w-5xl pt-12 lg:pt-0 mx-auto">
    <a
      href="/team"
      class="inline-flex items-center gap-2 text-sm text-neutral-500 hover:text-white transition-colors mb-12"
    >
      <svg class="size-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
        <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5L3 12m0 0l7.5-7.5M3 12h18" />
      </svg>
      Back to team
    </a>

    <div class="text-center mb-16">
      <div class="inline-block p-1 bg-gradient-to-r from-rose-400 via-rose-500 to-rose-600 mb-6">
        <div class="w-28 h-28 overflow-hidden border-4 border-neutral-950">
          <img src={member.photoUrl} alt={member.name} class="w-full h-full object-cover" />
        </div>
      </div>
      <h1 class="text-4xl font-serif font-medium text-white tracking-tight mb-2">{member.name}</h1>
      <p class="text-rose-400 font-medium">{member.position}</p>
    </div>

    <div class="relative mb-16">
      <div class="absolute left-1/2 -translate-x-px top-0 bottom-0 w-px bg-gradient-to-b from-rose-500/50 via-rose-500/20 to-transparent"></div>
      <div class="relative pl-8 before:absolute before:left-1/2 before:-translate-x-1/2 before:top-0 before:w-3 before:h-3 before:bg-rose-500 before:ring-4 before:ring-neutral-950">
        <p class="text-lg text-neutral-400 leading-relaxed max-w-md mx-auto text-center">
          Project Manager con pasión por liderar equipos hacia el éxito.
          Creo en la comunicación clara y la entrega de valor constante.
        </p>
      </div>
    </div>

    <div class="grid grid-cols-3 gap-6 mb-16">
      {achievements.map((item) => (
        <div class="text-center">
          <div class="text-3xl mb-2">{item.icon}</div>
          <div class="text-2xl font-bold text-white mb-1">{item.value}</div>
          <div class="text-xs text-neutral-500 uppercase tracking-wider">{item.title}</div>
        </div>
      ))}
    </div>

    <div class="mb-16">
      <h2 class="text-center text-sm text-neutral-500 uppercase tracking-wider mb-6">Expertise</h2>
      <div class="flex flex-wrap justify-center gap-3">
        {member.skills.map((skill) => (
          <span class="px-5 py-2.5 border border-rose-500/20 text-neutral-300 hover:bg-rose-500/10 hover:border-rose-500/40 transition-colors">
            {skill}
          </span>
        ))}
      </div>
    </div>

    <div class="text-center">
      <div class="inline-flex items-center gap-1 text-sm text-neutral-500 mb-4">
        <span class="w-8 h-px bg-neutral-700"></span>
        <span>Connect</span>
        <span class="w-8 h-px bg-neutral-700"></span>
      </div>
      <div class="flex justify-center">
        {member.linkedin && (
          <a
            href={member.linkedin}
            target="_blank"
            class="inline-flex items-center gap-2 px-6 py-3 bg-rose-500/10 border border-rose-500/20 text-rose-400 hover:bg-rose-500/20 transition-colors"
          >
            <LinkedinIcon class="size-4" />
            LinkedIn
          </a>
        )}
      </div>
    </div>
  </div>
</SidebarLayout>
